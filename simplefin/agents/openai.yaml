interface:
  display_name: "SimpleFIN"
  short_description: "Read SimpleFIN balances and transactions"
  default_prompt: "Use SimpleFIN to analyze my account balances and transactions."
dependencies:
  tools:
    - type: mcp
      value: connector_openai_library
      description: Persist and retrieve the private SimpleFIN credential state file
policy:
  allow_implicit_invocation: true
  products:
    - chatgpt
    - codex
